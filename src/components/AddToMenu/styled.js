import styled from "styled-components";

export const Container = styled.div`
    width: 80%;
    margin: auto;
`;

export const Submit = styled.input`
    color: ${(props) => props.theme.lilas2};
    background-color: ${(props) => props.theme.lilas3};
`;
